/**
 * @fileoverview Type declarations
 * @module core/types
 *
 * Global type declarations and module augmentations.
 * .d.ts files are automatically included by TypeScript.
 */

// Re-export any explicit types here if needed
