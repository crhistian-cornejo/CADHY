name: Release

# This workflow is disabled - we use the manual `bun release` script instead
# Tags pushed by the release script trigger release-app.yml

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "release" to confirm (this workflow is deprecated, use `bun release` locally instead)'
        required: true
        type: string

jobs:
  warn:
    name: Deprecated Workflow
    runs-on: ubuntu-latest
    if: github.event.inputs.confirm == 'release'
    steps:
      - name: Warning
        run: |
          echo "⚠️ This workflow is deprecated."
          echo ""
          echo "To create a release, run locally:"
          echo "  bun release"
          echo ""
          echo "This will:"
          echo "  1. Run validation (lint, typecheck, tests)"
          echo "  2. Bump versions"
          echo "  3. Update CHANGELOG.md"
          echo "  4. Create and push a git tag"
          echo "  5. Trigger release-app.yml to build binaries"
          echo ""
          echo "Binaries are published to: crhistian-cornejo/CADHY"
          exit 1
