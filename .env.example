# CADHY Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# ============================================================================

# ============================================================================
# AI PROVIDER PRIORITY (How CADHY selects AI providers)
# ============================================================================
# CADHY automatically selects the best available provider in this order:
#
#   1. Gemini OAuth (FREE) - Uses your Google account
#      Setup: npm install -g @google/gemini-cli && gemini
#
#   2. Ollama Local (FREE) - Run AI models on your machine
#      Setup: https://ollama.ai/download
#
#   3. BYOK Keys (YOUR COST) - Use your own API keys
#      Supported: OpenAI, Anthropic, Google AI, Ollama Cloud
#      Configure in: Settings -> AI Providers
#
#   4. AI Gateway (FALLBACK) - Rate-limited access via Vercel
#      Requires: VITE_AI_GATEWAY_API_KEY below
#
# ============================================================================

# ============================================================================
# AI Gateway Configuration (Fallback Only)
# ============================================================================
# The gateway is used ONLY when no other provider is available.
# Most users don't need this if they set up Gemini OAuth or Ollama.
#
# Get your key from: https://vercel.com/dashboard/ai
VITE_AI_GATEWAY_API_KEY=

# ============================================================================
# BYOK (Bring Your Own Key) - OPTIONAL
# ============================================================================
# You do NOT need to configure these in .env files!
# API keys are configured in the app UI and stored securely in OS Keychain.
#
# To add your own API keys:
#   1. Open CADHY
#   2. Go to Settings -> AI Providers
#   3. Add API keys for any provider you want to use
#
# Supported providers:
#   - OpenAI (GPT-4o, GPT-4o-mini)
#   - Anthropic (Claude Sonnet, Claude Haiku)
#   - Google AI (Gemini Pro, Gemini Flash)
#   - Ollama Cloud (access to large models)
#
# IMPORTANT: These are OPTIONAL. If you set up Gemini OAuth (free), you
# don't need any API keys at all!
# ============================================================================
